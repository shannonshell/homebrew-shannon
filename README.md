# Homebrew Tap for Shannon

Install Shannon with Homebrew:

```bash
brew tap shannonshell/shannon
brew trust shannonshell/shannon
brew install shannon
```

Shannon is a poly-shell built on Nushell with seamless Bash compatibility.
